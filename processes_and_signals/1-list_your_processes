#!/usr/bin/env bash
# Display a list of all currently running processes for all users, including those without a TTY
# in a user-oriented format, showing the process hierarchy.
# Using ps command with appropriate options to ensure comprehensive details
ps -eo user,stat,time,cmd --forest
