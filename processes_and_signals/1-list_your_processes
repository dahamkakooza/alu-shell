#!/usr/bin/env bash
# This script displays a list of currently running processes for all users, including those without a TTY, in a user-oriented format with process hierarchy.
ps -e -o user:10,comm:20,start:12 --forest
